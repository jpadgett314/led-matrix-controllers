import { DefaultController, HEIGHT, WIDTH } from 'led-matrix-controllers';

document.getElementById('app').textContent = `LED Matrix Dimensions: ${WIDTH}x${HEIGHT}`;

console.log('DefaultController available:', DefaultController);
